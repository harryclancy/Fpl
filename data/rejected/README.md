# Rejected research

Research that came back but failed validation. Nothing here is read by the
app.

It's kept because a run costs real money, and discarding the output
because one rule tripped means paying again to find out whether the rest
of it was fine. Each file records what was rejected, why, and the full
payload — so a near-miss can be inspected, and salvaged by hand if it's
worth it.
